let fname = 'Cristian';
let Iname = 'Ronaldo';
let age = prompt("Masukkan umur Cristian Ronaldo!");

// Cara lama
// let result = frane + ' ' + Iname + 'is' + age + 'years old';
// alert(result);

// Memakai template strings
let result = `${fname} ${Iname} is ${age} years old`;
alert(result);